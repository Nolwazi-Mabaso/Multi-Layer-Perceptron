# MLP Experiment Manager

A neural network experiment manager that trains and evaluates MLP (Multi-Layer Perceptron) models, automatically storing results in a JSON database for easy comparison and analysis.

## 🚀 Features

- Train MLP models on your CSV datasets
- Automatic experiment tracking with seed-based identification
- Persistent storage of results in JSON format
- Comprehensive performance metrics (Accuracy, F1 Score, Precision, Recall)
- Early stopping to prevent overfitting
- Confusion matrix for detailed analysis
- Cross-session result persistence

## 📦 Installation

1. **Download the executable**
   - Navigate to the `dist/MLP/` folder
   - Download `MLP.exe`

2. **Place the executable**
   - Move `MLP.exe` to your desired working directory
   - (Optional) Place your CSV datasets in the same folder for easy access

## 💻 Usage

### Running the Program

**Option A:** Double-click `MLP.exe`  
**Option B:** Run from command line:
```bash
./MLP.exe
# or
MLP.exe
